package com.wellnessmate.onboarding.domain;

/** Optional demographic answer used only in the private profile. @author TODO(team member) */
public enum SexOption { FEMALE, MALE, INTERSEX, PREFER_NOT_TO_SAY }
