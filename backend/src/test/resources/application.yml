spring:
  datasource:
    url: jdbc:h2:mem:wellness;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true

security:
  jwt:
    secret: test-only-jwt-secret-at-least-32-bytes-long
    ttl: PT2H
